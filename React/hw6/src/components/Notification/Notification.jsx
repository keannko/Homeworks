import s from "./Notification.module.scss";

const Notification = (props) => {
  return (
    <div className={s.mainDiv}>
      <h2>To place an order, add the product to the cart</h2>
    </div>
  );
};

export default Notification;
